{
  "name": "no-more-jobs",
  "version": "0.0.1",
  "description": "Simple task tracker built with web components",
  "repository": "https://github.com/mrtibbles/no-more-jobs",
  "author": "@MrTibbles",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "eslint": "^5.16.0",
    "husky": "^2.2.0",
    "parcel-bundler": "^1.12.3",
    "prettier": "^1.17.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint:js && git add ."
    }
  },
  "scripts": {
    "start:dev": "parcel app/index.html --log-level 2",
    "lint:js": "prettier --write app/**/*.js && eslint --fix \"app/**/*.js\""
  },
  "browserslist": [
    "since 2017-06"
  ]
}
